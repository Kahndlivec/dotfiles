" ═══════════════════════════════════════════════════════════════════════════
"  .neovintageousrc  —  Packages/User/.neovintageousrc
"  Reload without restarting:  ⌘⇧P → "NeoVintageous: Reload Config"
"
"  Assumes the HHKB layout in HHKB-LAYOUT.md:
"    Control key (row 3 far left) = ⌃ Control   ← cheap, home row, left pinky
"    Bottom-left Alt slot         = ⌘ Command   ← stock-Mac position
"    ◇ left of Space              = Fn          ← Fn+hjkl = arrows, everywhere
"    Bottom-right Alt slot        = ⌥ Option    ← corner key; never bind to it
"
"  ⌥ can't combine with the right hand at all, so Vim replaces every macOS
"  ⌥-motion chord: b/w for ⌥←/⌥→, db and i_CTRL-W for ⌥⌫, dw for ⌥⌦,
"  {/} for ⌥↑/⌥↓, viw/vw for ⌥⇧←/→. Inside Sublime you never touch ⌥.
" ═══════════════════════════════════════════════════════════════════════════

" ── Core options ───────────────────────────────────────────────────────────
set ignorecase                  " /foo matches Foo
set smartcase                   " ...but /Foo matches only Foo
set incsearch
set hlsearch                    " cleared by <leader>h
set wrapscan
set scrolloff=5                 " cursor never touches the screen edge
set relativenumber
set number                      " hybrid: absolute current line, relative around
set belloff=all
set clipboard=unnamed           " y and p use the macOS system clipboard
set winaltkeys=menu

let g:highlightedyank = 1
let g:highlightedyank_duration = 300

" Built-in NeoVintageous plugins:
"   surround (cs"' ds( ysiw)) · commentary (gcc gcap) · unimpaired (]q [q ]b)
"   abolish (crs crc cru) · targets (ci( from anywhere on the line)
let g:sneak = 1                 " s{char}{char} — 2-char jump

" ── Leader = Space ─────────────────────────────────────────────────────────
" Biggest key on the board, thumbs already parked. Still types a space in
" insert mode.
let mapleader=<Space>

" ═══════════════════════════════════════════════════════════════════════════
"  ESCAPE
"  HHKB puts Esc at top-left where ` normally lives — the best Esc placement
"  that exists. Use it. With Control back on home row, C-[ is also a real
"  option now. jk is the third fallback; pick one and stop thinking about it.
" ═══════════════════════════════════════════════════════════════════════════
" [jk removed - real ~200ms stall on every j you type in
"  insert mode, and j is a C++ loop variable. Use Ctrl-[.]
" inoremap jk <Esc>
" [jk removed - real ~200ms stall on every j you type in
"  insert mode, and j is a C++ loop variable. Use Ctrl-[.]
" cnoremap jk <Esc>

" ═══════════════════════════════════════════════════════════════════════════
"  hjkl — no escape hatches
"  Your Fn layer now puts real arrow keycodes on Fn+hjkl, so arrows and hjkl
"  are literally the same finger positions. In normal mode you just drop the
"  Fn. Which means there is zero reason for an arrow key to ever fire here.
"  Delete these six lines in ~2 weeks; you won't notice they're gone.
" ═══════════════════════════════════════════════════════════════════════════
nnoremap <Up>    <Nop>
nnoremap <Down>  <Nop>
nnoremap <Left>  <Nop>
nnoremap <Right> <Nop>
vnoremap <Up>    <Nop>
vnoremap <Down>  <Nop>

" Move by visual line on wrapped lines — matches what your eyes see.
nnoremap j gj
nnoremap k gk

" H / L as line start / end. `^` and `$` are shifted, awkward keys you'd
" otherwise hit a hundred times an hour.
" [H/L reverted to vanilla: screen-top / screen-bottom, with M in
"  the middle. They mean that in every vim you did not configure -
"  ssh, someone else's machine, less, man, vimium. Use 0 and $.
"  Delete the leading " on the six lines below to restore.]
" nnoremap H ^
" nnoremap L $
" vnoremap H ^
" vnoremap L $
" onoremap H ^
" onoremap L $

" ═══════════════════════════════════════════════════════════════════════════
"  CONTROL CHORDS — the reason Control went back on the home row
"  These are the in-flow, burst-fired keys. Now they cost nothing.
" ═══════════════════════════════════════════════════════════════════════════

" Half-page scroll, cursor recentred. Fired in bursts while reading code —
" this is THE chord that justifies Control's position.
nnoremap <C-d> <C-d>zz
nnoremap <C-u> <C-u>zz

" Search results and big jumps land centred too.
nnoremap n nzzzv
nnoremap N Nzzzv
nnoremap G Gzz
nnoremap <C-o> <C-o>zz
nnoremap <C-i> <C-i>zz

" Window nav (<C-h/j/k/l>) is NOT mapped here on purpose — the Sublime keymap
" binds those to Origami's travel_to_pane instead, so they work in insert mode
" too, not just normal. Same keys, same result, one more mode.

" Splits without the <C-w> prefix.
nnoremap <C-w>v :vsplit<CR>
nnoremap <C-w>s :split<CR>

" ═══════════════════════════════════════════════════════════════════════════
"  EDITING QUALITY OF LIFE
" ═══════════════════════════════════════════════════════════════════════════

" Keep cursor in place when joining lines.
nnoremap J mzJ`z

" Don't clobber the yank register when pasting over a selection.
vnoremap p "_dP

" Delete WITHOUT losing what you just yanked. Trips up every Vim beginner.
nnoremap <leader>d "_d
vnoremap <leader>d "_d

" Indent and stay in visual mode — repeatable.
vnoremap < <gv
vnoremap > >gv

" Move selected lines up/down. (⌥j/⌥k would collide with macOS; leader is safe.)
vnoremap <leader>j :m '>+1<CR>gv=gv
vnoremap <leader>k :m '<-2<CR>gv=gv

nnoremap <leader>h :nohlsearch<CR>

" ═══════════════════════════════════════════════════════════════════════════
"  FILES / WINDOWS  (leader = Space)
" ═══════════════════════════════════════════════════════════════════════════
nnoremap <leader>w :w<CR>
nnoremap <leader>q :q<CR>
nnoremap <leader>x :wq<CR>
nnoremap <leader>v :vsplit<CR>
nnoremap <leader>s :split<CR>

" ═══════════════════════════════════════════════════════════════════════════
"  COMPETITIVE PROGRAMMING
" ═══════════════════════════════════════════════════════════════════════════
" Build with sanitizers (same as ⌘B).
nnoremap <leader>b :Build<CR>

" Wipe the buffer and drop into insert at the top — then type `cp` + Tab for
" the contest template. Two steps on purpose: <leader>t is destructive.
nnoremap <leader>t ggdGi

" Select the whole buffer.
nnoremap <leader>a ggVG

" === appended by finish.sh — do not duplicate ===
" ═══════════════════════════════════════════════════════════════════════════
"  APPEND THIS TO THE BOTTOM of your existing Packages/User/.neovintageousrc
"  Do not replace anything above — every line below is new.
"
"  These are the mappings that existed in VS Code but had no key in Sublime.
"  Your LSP-clangd commands were palette-only; now they're on <leader>, on the
"  same keys as VS Code's init.lua.
"
"  Checked for collisions against your current rc: none. Nothing here starts
"  with <leader>d, <leader>b, <leader>t, <leader>v, <leader>s, <leader>h,
"  <leader>w, <leader>q, <leader>x, <leader>a, <leader>j or <leader>k, so
"  every one of your existing maps still fires on the first keystroke.
" ═══════════════════════════════════════════════════════════════════════════

" ── Small additions ────────────────────────────────────────────────────────
" Q replays macro q instead of opening Ex mode, which nobody wants.
nnoremap Q @q
" Close every open file. Pairs with your <leader>q.
nnoremap <leader>Q :CloseAll<CR>

" ── Files and search ───────────────────────────────────────────────────────
" Space Space — file picker. Two taps for the thing you do most.
nnoremap <leader><leader> :ShowOverlay overlay=goto show_files=true<CR>
nnoremap <leader>ff :ShowOverlay overlay=goto show_files=true<CR>
nnoremap <leader>fg :ShowPanel panel=find_in_files<CR>
nnoremap <leader>fs :ShowOverlay overlay=goto text=@<CR>
nnoremap <leader>fS :GotoSymbolInProject<CR>
nnoremap <leader>e :ToggleSideBar<CR>

" ── LSP / clangd — these had no keybinding at all before ───────────────────
nnoremap <leader>fm :LspFormatDocument<CR>
nnoremap <leader>rn :LspSymbolRename<CR>
nnoremap <leader>ca :LspCodeActions<CR>
vnoremap <leader>ca :LspCodeActions<CR>
nnoremap <leader>li :LspSymbolImplementation<CR>
nnoremap <leader>lt :LspSymbolTypeDefinition<CR>
nnoremap ]d :LspNextDiagnostic<CR>
nnoremap [d :LspPrevDiagnostic<CR>

" ── Git gutter navigation — NOT mapped, on purpose ─────────────────────────
" NeoVintageous already implements ]c and [c natively (forward/backward to
" start of change). Overriding them would only replace a working built-in
" with a slightly different one. VS Code's init.lua maps them to the same
" thing, so the keys match anyway.

" ═══════════════════════════════════════════════════════════════════════════
"  NOT added, deliberately — Sublime has no such feature, so there is no
"  habit to lose: debugging, source-control view, integrated terminal,
"  notebooks. In VS Code those live on the ⌘ layer (⌘B, ⌘⇧D, ⌘;, ⌘K D,
"  ⌘K T, ⌘⇧G), never on <leader>, precisely so <leader>d stays a
"  black-hole-delete operator in both editors.
" ═══════════════════════════════════════════════════════════════════════════
